<p id="notice"><%= notice %></p>

<table class="table table-striped table-bordered text-center">
  <thead>
    <tr>
      <th>Name</th>
      <td><%= current_user.name %></td>
    </tr>
  </thead>

  <tbody>
      <tr>
        <th>Email</th>
        <td><%= current_user.email %></td>
      </tr>
  </tbody>
</table>

<%= link_to 'My Tasks', tasks_path, class:"btn btn-success" %>
